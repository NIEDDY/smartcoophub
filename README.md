
  # Smart Cooperative Hub PROJECT

  This is a code bundle for Smart Cooperative Hub  
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  